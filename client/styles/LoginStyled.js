import styled from "styled-components";

export const LoginStyled = styled.div`
  position: absolute;
  right: 0;
  left: 0;
  top: 0;
  bottom: 0;
  background: linear-gradient(90deg, #454dcc 0%, rgba(0, 255, 255) 75%);

  display: flex;
  align-items: center;
  justify-content: center;
`;
